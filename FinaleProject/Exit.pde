class Exit {
  //data
  float exitX, exitY;
  float playerX, playerY;
  float cellWidth, cellHeight;

  boolean isWin;

  //constructor
  Exit(DrawLevel currentLevel){
    exitX = currentLevel.exitX; 
    exitY = currentLevel.exitY;
  }

  //behaviour
}